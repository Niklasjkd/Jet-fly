<div class="container">
  <div class="card-deck">
    <% @planes.each do |plane| %>
      <div class="card" style="width: 18rem;">
        <%= link_to plane_path(plane, destination: params[:destination]) do %>
        <%= cl_image_tag("ckm3sqhwfzuwusyrgroh", crop: :fill, class: "card-img-top") %>
        <% end %>
        <div class="card-body">
          <h2 class="card-title">
            <%= plane.description %>
          </h2>
        </div>
        <div class="card-text">
          <p> <%= "Destination: #{plane.location}" %></p>
        </div>
    </div>
    <% end %>
  </div>
</div>
