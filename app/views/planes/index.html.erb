<div class="container">
  <div class="plane-deck">
    <% @planes.each do |plane| %>
<!--
        <div class="card mb-3" style="max-width: 540px; height: 180px">
      <div class="row no-gutters">
          <%= cl_image_tag(plane.image, crop: :fill, class: "card-img col-md-6") %>

        <div class="col-md-6">
          <div class="card-body">
            <h5 class="card-title"><%= plane.description %></h5>
            <p class="card-text">
              <%= plane.location %> ->
              <%= params[:destination] %>
            </p>
            <p class="card-text"><small class="text-muted">-</small></p>
          </div>
        </div>
      </div>
    </div> -->
<%= link_to plane_path(plane, destination: params[:destination], date: params[:date]) do %>
    <div class="card plane mb-3" style="border: 0px; border-radius: 4px;">

      <%= cl_image_tag(plane.image, crop: :fill, class: "w-100", style: "border-radius: 4px 4px 0px 0px; height: 290px;") %>

      <div class="card-body w-100 text-body">
        <h5 class="card-title"> <%= plane.description %> </h5>
        <p class="card-text"><small class="text-muted"><%= plane.location %> to <%= params[:destination] %></small></p>
      </div>
    </div>
    <% end %>
    <% end %>
  </div>
</div>
